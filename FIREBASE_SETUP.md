# 🔥 Настройка Firebase для Хитас для вундеркиндов

## Проблема

При попытке синхронизации прогресса с облаком появляется ошибка:
```
❌ Ошибка загрузки из облака
```

## Причина

**Правила безопасности Firestore не настроены!** По умолчанию Firebase блокирует все запросы к базе данных для безопасности.

## Решение

### 1. Откройте Firebase Console

1. Перейдите на https://console.firebase.google.com/
2. Выберите проект **chitas-for-kids**

### 2. Настройте Firestore Database

1. В боковом меню выберите **Firestore Database**
2. Если база данных еще не создана:
   - Нажмите **Create database**
   - Выберите **Start in production mode**
   - Выберите регион (рекомендуется **europe-west**)
   - Нажмите **Enable**

### 3. Установите правила безопасности

1. В Firestore Database перейдите на вкладку **Rules**
2. Скопируйте содержимое файла `firestore.rules` из этого репозитория
3. Вставьте в редактор правил:

```javascript
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Правила для коллекции userProgress
    // Каждый пользователь может читать и писать только свои данные
    match /userProgress/{userId} {
      // Пользователь может читать и изменять только свой документ
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // Запретить доступ ко всем остальным коллекциям по умолчанию
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
```

4. Нажмите **Publish** (Опубликовать)

### 4. Проверьте Authentication

1. В боковом меню выберите **Authentication**
2. Нажмите **Get Started** (если Authentication еще не настроена)
3. Перейдите на вкладку **Sign-in method**
4. Включите провайдеры:

#### Email/Password:
   - Нажмите **Email/Password**
   - Переключите **Enable** в положение ON
   - Нажмите **Save**

#### Google Sign-In:
   - Нажмите **Google**
   - Переключите **Enable** в положение ON
   - Укажите **Project support email** (ваш email)
   - Нажмите **Save**

### 5. Настройте домены для Google Sign-In

**ВАЖНО!** Без этого шага Google Sign-In не будет работать.

1. В **Authentication** → вкладка **Settings**
2. Прокрутите до **Authorized domains**
3. Убедитесь, что добавлены домены:
   - ✅ `mychitas.app` (ваш production домен)
   - ✅ `localhost` (для локального тестирования)

4. Если домен отсутствует:
   - Нажмите **Add domain**
   - Введите домен (например, `mychitas.app`)
   - Нажмите **Add**

## Что делают правила безопасности?

```javascript
match /userProgress/{userId} {
  allow read, write: if request.auth != null && request.auth.uid == userId;
}
```

Эти правила:
- ✅ Разрешают пользователям читать и писать **только свои данные**
- ✅ Требуют **аутентификацию** (вход в систему)
- ✅ Проверяют, что `userId` в базе данных совпадает с ID вошедшего пользователя
- ❌ Блокируют доступ к данным других пользователей

## Проверка работы

После настройки правил и Authentication:

### Проверка Google Sign-In:
1. Откройте приложение https://mychitas.app
2. Перейдите в **Профиль** 👤
3. Нажмите **Войти**
4. Выберите **🔐 Войти через Google**
5. Выберите аккаунт Google
6. Должен произойти успешный вход

### Проверка синхронизации:
1. После входа перейдите в **Настройки** ⚙️
2. Нажмите **☁️ Синхронизация**
3. Должно появиться: **✅ Прогресс синхронизирован с облаком!**

### Проверка загрузки:
1. В **Настройки** нажмите **📥 Загрузить из облака**
2. Должно появиться: **✅ Прогресс загружен из облака!**

## Отладка ошибок

### Ошибка синхронизации

Если ошибка все еще возникает:

1. Откройте консоль браузера (F12)
2. Посмотрите на детальные логи:
   ```
   ❌ Error syncing to Firebase: FirebaseError
   Error code: permission-denied
   ```

3. Проверьте:
   - ✅ Пользователь вошел в систему
   - ✅ Правила опубликованы в Firestore
   - ✅ Firestore Database создана

### Google Sign-In не работает

Если Google Sign-In не работает:

1. **Проверьте консоль браузера (F12)**:
   - Ошибка `auth/popup-blocked` → Разрешите popup окна
   - Ошибка `auth/unauthorized-domain` → Добавьте домен в Authorized domains
   - Ошибка `auth/operation-not-allowed` → Включите Google Sign-In в Authentication

2. **Проверьте настройки Firebase**:
   - Authentication → Sign-in method → Google: **Enabled**
   - Authentication → Settings → Authorized domains: добавлен ваш домен

3. **Проверьте код**:
   - Откройте консоль и введите: `console.log(window.authManager)`
   - Должен показать объект AuthManager
   - Введите: `console.log(typeof googleProvider)`
   - Должен показать `"object"`

4. **Очистите кеш браузера**:
   - Нажмите Ctrl+Shift+Delete
   - Очистите кеш и cookies
   - Обновите страницу

## Дополнительная информация

### Структура данных в Firestore

```
userProgress (collection)
  └── {userId} (document)
      ├── score: number
      ├── stars: number
      ├── completed: object
      ├── settings: object
      └── lastSync: string (ISO date)
```

### Как посмотреть данные

1. Firebase Console → **Firestore Database**
2. Вкладка **Data**
3. Коллекция `userProgress`
4. Документы с ID пользователей

## CLI команды (опционально)

Если у вас установлен Firebase CLI:

```bash
# Установка Firebase CLI
npm install -g firebase-tools

# Вход
firebase login

# Инициализация проекта
firebase init firestore

# Деплой правил
firebase deploy --only firestore:rules
```

## Контакты

Если проблема не решена, откройте issue на GitHub или свяжитесь с разработчиком.
