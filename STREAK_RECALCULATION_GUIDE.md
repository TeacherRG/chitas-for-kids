# Руководство по пересчету стриков пользователей

## 📋 Описание проблемы

В некоторых случаях у пользователей может возникнуть ситуация, когда:
- Пройдено 10+ дней активности
- Но `currentStreak` = 0
- И `maxStreak` = 0

Это происходит когда стрики не были корректно инициализированы или обновлены в базе данных.

## ✅ Решение: Автоматический пересчет

**ВНИМАНИЕ:** Начиная с последнего обновления, пересчет стриков происходит **автоматически** при загрузке сайта!

### Как это работает:

1. При загрузке сайта mychitas.app автоматически запускается функция `recalculateStreaksOnce()`
2. Функция проверяет флаг `streaksRecalculated` в данных пользователя
3. Если флаг не установлен (пересчет еще не был):
   - Вычисляет правильные значения `currentStreak` и `maxStreak` на основе `completed` данных
   - Обновляет значения в state
   - Сохраняет в localStorage и Firebase
   - Устанавливает флаг `streaksRecalculated = true`
4. При следующих загрузках сайта пересчет не происходит (флаг уже установлен)

**Это означает, что каждый пользователь автоматически получит правильные стрики при первом входе после обновления!**

### 📊 Просмотр логов пересчета

Чтобы увидеть результаты автоматического пересчета:
1. Откройте консоль браузера (F12)
2. Загрузите или перезагрузите страницу mychitas.app
3. В консоли вы увидите:
   - `🔄 Выполняется одноразовый пересчет стриков...` - начало процесса
   - `📊 Пересчет стриков завершен:` - результаты с старыми и новыми значениями
   - `✅ Стрики успешно пересчитаны и сохранены` - подтверждение

Или, если пересчет уже был выполнен ранее:
   - `ℹ️ Стрики уже пересчитаны ранее` - пересчет не требуется

## 🔧 Дополнительные способы (для администраторов)

Если нужно пересчитать стрики для всех пользователей сразу (массовая операция):

### Способ 1: Через административную панель

1. Откройте файл `admin-recalculate-streaks.html` в браузере
2. Авторизуйтесь в Firebase (через консоль браузера или отдельную страницу авторизации)
3. Нажмите кнопку **"🚀 Запустить пересчет"**
4. Подтвердите действие
5. Дождитесь завершения операции
6. Проверьте лог выполнения

**Преимущества:**
- Удобный визуальный интерфейс
- Детальный лог в реальном времени
- Подтверждение перед выполнением
- Статус выполнения операции

### Способ 2: Через консоль браузера

1. Откройте сайт приложения в браузере
2. Откройте консоль разработчика (F12)
3. Вставьте содержимое файла `recalculate-streaks.js` в консоль
4. Выполните команду:
   ```javascript
   await recalculateAllUserStreaks()
   ```
5. Дождитесь завершения операции
6. Проверьте результаты в консоли

**Преимущества:**
- Не требует дополнительных файлов
- Можно запустить на любой странице приложения
- Прямой доступ к результатам в консоли

## 📊 Что делает функция?

### Алгоритм расчета currentStreak (текущий стрик)

1. Получает все даты с активностью из `completed`
2. Сортирует их от новых к старым
3. Начинает с сегодняшнего дня и идет назад
4. Считает дни подряд с активностью
5. **Пропускает субботы** (не сбрасывает стрик)
6. Останавливается при первом пропущенном дне (кроме субботы)

### Алгоритм расчета maxStreak (максимальный стрик)

1. Получает все даты с активностью
2. Проходит по всей истории активности
3. Находит самую длинную серию дней подряд
4. **Учитывает пропуск субботы** как допустимый
5. Сравнивает со старым значением `maxStreak`
6. Берет максимальное значение (защита от потери рекордов)

### Что обновляется в базе данных?

Для каждого пользователя в коллекции `userProgress`:
```javascript
{
  currentStreak: <новое значение>,
  maxStreak: <новое значение>,
  lastStreakRecalculation: <timestamp>
}
```

## 📝 Пример вывода

```
🔄 Начинаем пересчет стриков для всех пользователей...

📥 Загружаем данные пользователей из Firebase...
📊 Найдено пользователей: 5

[1/5] 👤 Обработка пользователя: user123abc
  📊 Старые значения: currentStreak=0, maxStreak=0
  ✨ Новые значения: currentStreak=12, maxStreak=15
  ✅ Обновлено в Firebase

[2/5] 👤 Обработка пользователя: user456def
  📊 Старые значения: currentStreak=5, maxStreak=5
  ℹ️ Обновление не требуется (стрики уже корректны)

...

============================================================
📊 ИТОГОВЫЙ ОТЧЕТ
============================================================
✅ Успешно обработано: 5 пользователей
🔄 Обновлено в базе: 3 пользователей
❌ Ошибок: 0
============================================================

📋 ДЕТАЛЬНЫЙ ЛОГ ИЗМЕНЕНИЙ:
┌─────────┬─────────────┬──────────────────┬──────────────────┬───────────────┬───────────────┬────────────────┐
│ (index) │   userId    │ oldCurrentStreak │ newCurrentStreak │  oldMaxStreak │  newMaxStreak │ completedDays  │
├─────────┼─────────────┼──────────────────┼──────────────────┼───────────────┼───────────────┼────────────────┤
│    0    │ 'user123'   │        0         │        12        │       0       │      15       │       20       │
│    1    │ 'user456'   │        0         │         7        │       0       │      10       │       15       │
│    2    │ 'user789'   │        3         │         5        │       5       │       8       │       12       │
└─────────┴─────────────┴──────────────────┴──────────────────┴───────────────┴───────────────┴────────────────┘
```

## ⚠️ Важные замечания

1. **Одноразовое использование**: Функция предназначена для однократного запуска для исправления существующих данных

2. **Требуется авторизация**: Необходим авторизованный пользователь с правами доступа к коллекции `userProgress`

3. **Безопасность данных**:
   - Функция только обновляет `currentStreak` и `maxStreak`
   - Не изменяет `completed`, `score`, `stars` и другие данные
   - `maxStreak` берется как максимум из старого и нового значения (рекорды не теряются)

4. **Firestore Rules**: Убедитесь, что правила Firestore разрешают обновление документов в коллекции `userProgress`

5. **Производительность**:
   - Функция обрабатывает всех пользователей последовательно
   - Для большого количества пользователей (1000+) может потребоваться несколько минут
   - Рекомендуется запускать в период низкой нагрузки

## 🔍 Проверка результатов

После выполнения функции проверьте:

1. **В консоли**: Итоговый отчет с количеством обработанных пользователей
2. **В Firebase Console**: Откройте Firestore → `userProgress` → выберите пользователя → проверьте поля:
   - `currentStreak` - должен соответствовать текущей серии дней
   - `maxStreak` - должен быть >= `currentStreak`
   - `lastStreakRecalculation` - timestamp выполнения миграции

3. **В приложении**: Попросите пользователей проверить их стрики в профиле

## 🐛 Устранение проблем

### Firebase не инициализирован
```
❌ Firebase Firestore не инициализирован!
```
**Решение**: Убедитесь, что `firebase-config.js` загружен и Firebase инициализирован

### Ошибка прав доступа
```
❌ Error: permission-denied
```
**Решение**: Проверьте правила Firestore. Добавьте правило для авторизованных пользователей:
```javascript
allow read, write: if request.auth != null;
```

### Нет подключения к интернету
```
❌ Error: unavailable
```
**Решение**: Проверьте подключение к интернету и попробуйте снова

## 📁 Файлы

- `recalculate-streaks.js` - Основная функция пересчета стриков
- `admin-recalculate-streaks.html` - Административная панель с UI
- `STREAK_RECALCULATION_GUIDE.md` - Это руководство

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера на наличие ошибок
2. Убедитесь в правильной настройке Firebase
3. Проверьте права доступа в Firestore Rules
4. Свяжитесь с администратором системы
