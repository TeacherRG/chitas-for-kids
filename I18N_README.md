# Многоязычность / Multi-language Support

## Поддерживаемые языки / Supported Languages

- 🇷🇺 Русский (Russian) - оригинальный язык
- 🇬🇧 English (Английский)
- 🇺🇦 Українська (Украинский)
- 🇩🇪 Deutsch (Немецкий)
- 🇮🇱 עברית (Иврит)

## Как это работает / How it works

### Автоматическое определение языка
Приложение автоматически определяет язык браузера при первой загрузке:
- Если язык браузера - русский → используется русский
- Если язык поддерживается → используется этот язык
- Во всех остальных случаях → используется английский

### Перевод с помощью Gemini API
Все тексты переводятся с помощью Gemini API с учетом особенностей религиозного контента:
- Еврейские термины и имена сохраняются без изменений
- Названия Торы и праздников не переводятся
- Сохраняются все эмодзи и специальные символы

### Кэширование переводов
Переводы сохраняются в localStorage браузера для быстрого доступа и экономии API запросов.

**Преимущества кэширования:**
- ⚡ Мгновенная загрузка уже переведенных текстов
- 💰 Экономия API запросов к Gemini
- 🔄 Автоматическая очистка при переполнении (> 4MB)
- 📊 Версионирование кэша для обновлений

**Что кэшируется:**
- Тексты интерфейса (кнопки, меню, заголовки)
- Контент разделов (Хумаш, Теилим, Тания и т.д.)
- Тексты игр и вопросов
- Объяснения и инструкции

**Статистика кэша:**
Открой консоль браузера (F12), чтобы увидеть количество сохраненных переводов и размер кэша.

## Использование / Usage

### Смена языка
Нажмите на иконку 🌐 в левом верхнем углу шапки и выберите нужный язык из выпадающего меню.

### Очистка кэша переводов
Откройте консоль браузера (F12) и выполните:
```javascript
window.i18n.clearCache();
```

## Для разработчиков / For Developers

### Добавление переводимого элемента
Добавьте атрибут `data-i18n` к HTML элементу:
```html
<p data-i18n="my-key">Текст на русском</p>
```

### Программный перевод текста
```javascript
const translated = await window.i18n.translateWithGemini('Текст', 'en');
```

### Добавление нового языка
1. Откройте `i18n.js`
2. Добавьте язык в объект `LANGUAGES`:
```javascript
fr: { name: 'French', flag: '🇫🇷', nativeName: 'Français' }
```

## API Ключ
API ключ Gemini хранится в файле `i18n.js`. Для безопасности рекомендуется переместить его в переменную окружения в production.

## Особенности религиозного контента

Промпт для Gemini настроен на корректный перевод еврейского образовательного контента:
- ב״ה всегда сохраняется без изменений
- Еврейские даты не переводятся
- Имена праздников и разделов Торы сохраняются
- Транслитерированные термины не переводятся

## Поддержка RTL для иврита
При выборе иврита:
- Направление текста меняется на RTL (справа налево)
- Расположение элементов интерфейса зеркалируется
- ב״ה и селектор языка меняются местами
