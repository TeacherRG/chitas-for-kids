# Инструкция по деплою в Firebase

## 🎯 Что нужно задеплоить:

1. ✅ **Firestore Rules** (критично!) - валидация данных
2. ✅ **Hosting** - код с исправлениями XSS, Rate Limiting, Security Headers

---

## 🚀 ВАРИАНТ 1: Через Firebase Console (БЫСТРО!)

### Firestore Rules

**Время: 2 минуты**

1. Откройте: https://console.firebase.google.com/
2. Выберите проект **"chitas-for-kids"**
3. **Firestore Database** → **Rules**
4. Скопируйте весь текст из файла `firestore.rules`
5. Вставьте в редактор (заменив старые правила)
6. Нажмите **"Publish"**
7. ✅ Готово!

### Hosting

**Если Hosting уже настроен:**
1. **Hosting** → **Deploy**
2. Загрузите файлы вручную

**Если Hosting НЕ настроен:**
- Используйте Вариант 2 или 3

---

## 💻 ВАРИАНТ 2: Автоматический скрипт

**Время: 5 минут**

```bash
cd /home/user/chitas-for-kids
./deploy.sh
```

Скрипт автоматически:
- Установит Firebase CLI
- Выполнит вход в аккаунт
- Задеплоит Firestore Rules
- (Опционально) Задеплоит Hosting

---

## 🛠️ ВАРИАНТ 3: Вручную через CLI

### Шаг 1: Установка Firebase CLI

```bash
# Установка
npm install -g firebase-tools

# Проверка
firebase --version
```

### Шаг 2: Вход в Firebase

```bash
firebase login
```

Откроется браузер для авторизации.

### Шаг 3: Инициализация проекта

```bash
cd /home/user/chitas-for-kids

# Создание .firebaserc
cat > .firebaserc << 'EOF'
{
  "projects": {
    "default": "chitas-for-kids"
  }
}
EOF
```

### Шаг 4: Деплой

```bash
# Только Firestore Rules (быстро)
firebase deploy --only firestore:rules

# Только Hosting
firebase deploy --only hosting

# Всё сразу
firebase deploy
```

---

## ✅ Проверка после деплоя

### 1. Firestore Rules

**Через Console:**
1. https://console.firebase.google.com/
2. Firestore Database → Rules
3. Проверьте, что видите функции: `isValidUserProgress()`, `isOwner()`, и т.д.

**Через Rules Playground:**
1. Firestore Database → Rules → **Rules Playground**
2. Попробуйте тест:
   - Location: `/userProgress/testUserId`
   - Authenticated: ✅ Yes
   - UID: `testUserId`
   - Should: ✅ Allow

### 2. Hosting

**Проверка Security Headers:**
1. Откройте ваш сайт
2. DevTools (F12) → Network
3. Обновите страницу (F5)
4. Кликните на первый запрос (index.html)
5. Проверьте Headers:

Должно быть:
```
Content-Security-Policy: default-src 'self'...
Strict-Transport-Security: max-age=31536000...
X-Frame-Options: DENY
X-Content-Type-Options: nosniff
```

### 3. Rate Limiting

**Тест:**
1. Попробуйте войти с неправильным паролем 5 раз
2. На 6-й раз должна быть блокировка: "Слишком много попыток входа. Повторите через 15 минут."

### 4. XSS защита

**Тест:**
1. Создайте пользователя с именем: `<script>alert('XSS')</script>`
2. Имя должно отображаться как текст, а не выполниться

---

## 🆘 Решение проблем

### Ошибка: "Firebase project not found"

```bash
# Проверьте .firebaserc
cat .firebaserc

# Должно быть:
{
  "projects": {
    "default": "chitas-for-kids"
  }
}
```

### Ошибка: "Permission denied"

```bash
# Перелогиньтесь
firebase logout
firebase login
```

### Ошибка: "Invalid rules syntax"

```bash
# Проверка синтаксиса БЕЗ деплоя
firebase deploy --only firestore:rules --dry-run
```

### Ошибка: "Command not found: firebase"

```bash
# Переустановка
npm uninstall -g firebase-tools
npm install -g firebase-tools

# Если не помогло, добавьте в PATH
export PATH=$PATH:$(npm bin -g)
```

---

## 📋 Чек-лист перед деплоем

- [ ] Файл `firestore.rules` содержит новые правила с валидацией
- [ ] Файл `firebase.json` содержит security headers
- [ ] Файл `auth-ui.js` содержит `escapeHtml()` функцию
- [ ] Файл `auth-manager.js` содержит Rate Limiting
- [ ] У вас есть доступ к проекту "chitas-for-kids" в Firebase

---

## 🎓 Рекомендации

### После первого деплоя:

1. **Протестируйте приложение:**
   - Вход/регистрация работает
   - Сохранение прогресса работает
   - Rate limiting срабатывает

2. **Мониторинг:**
   - Firebase Console → Usage
   - Проверяйте количество запросов
   - Следите за ошибками

3. **Backup:**
   - Firestore → Import/Export
   - Регулярно делайте backup данных

### Регулярный деплой:

```bash
# Короткая команда для быстрого деплоя
cd /home/user/chitas-for-kids && firebase deploy
```

---

## 📞 Нужна помощь?

1. Firebase Documentation: https://firebase.google.com/docs
2. Firestore Rules Guide: См. `FIRESTORE_RULES_GUIDE.md`
3. Security Audit: См. `SECURITY_AUDIT.md`

---

## ✨ Готово!

После деплоя ваше приложение будет защищено от:
- ✅ XSS атак
- ✅ Brute force атак
- ✅ Некорректных данных в БД
- ✅ Clickjacking
- ✅ MIME sniffing
- ✅ MitM атак (HSTS)

**Оценка безопасности: 8.5/10** 🔒
